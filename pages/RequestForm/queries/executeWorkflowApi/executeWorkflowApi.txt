{
	"requestId": "{{ appsmith.store.UUID}}",
	"workflowType": "{{ "firewallcheck" }}",
	"targetEnvironment": "{{en.selectedOptionValue }}",
	"requestData": "{{ 
	JSON.stringify({
		sourceIp: srcIp.text,
		destinationIp: destIp.text
	}) 
	}}"
}